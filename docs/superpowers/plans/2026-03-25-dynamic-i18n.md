# Dynamic Game Data i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让所有来自服务端的动态游戏字符串（技能名、装备名称、词缀/mods、警告）都走 CSV i18n 翻译，支持含数字的词缀模板匹配（`{0}`、`{1}` 占位符）。

**Architecture:** 前端 `useI18n` hook 已加载全量翻译 Map，只需增强 `t()` 函数：直接查找失败时，把字符串中所有数字替换为 `{0}`, `{1}`... 再查一次；若命中则把数字注回翻译结果。各渲染组件调用 `useI18n` 并对动态字符串应用 `t()`。

**Tech Stack:** React hooks, TypeScript, 现有 `/api/i18n` + `useI18n` 基础设施

---

### Task 1: 增强 `useI18n.ts` — 支持数字归一化查找

**Files:**

- Modify: `web/src/client/hooks/useI18n.ts`

- [ ] **Step 1: 修改 `t()` 函数，增加归一化分支**

当 `t(str)` 无额外参数且直接查找失败时，提取字符串中所有数字（含正负号、小数）并替换为顺序占位符，再次查找 Map；若命中，则把提取的数字注回翻译模板。

将 `useI18n.ts` 的完整内容替换为：

```typescript
import { useState, useEffect } from "react";

let i18nMap: Map<string, string> | null = null;

async function loadI18nMap(): Promise<Map<string, string>> {
  if (i18nMap) return i18nMap;
  try {
    const res = await fetch("/api/i18n");
    if (!res.ok) throw new Error(`${res.status}`);
    const data: Record<string, string> = await res.json();
    i18nMap = new Map(Object.entries(data));
  } catch {
    i18nMap = new Map();
  }
  return i18nMap;
}

export function useI18n() {
  const [map, setMap] = useState<Map<string, string>>(new Map());

  useEffect(() => {
    loadI18nMap().then(setMap);
  }, []);

  function t(key: string, ...args: (string | number)[]): string {
    let text = map.get(key);
    let effectiveArgs: (string | number)[] = args;

    // 直接查找失败且调用方没有传入显式参数时，尝试数字归一化查找
    if (text === undefined && args.length === 0) {
      const nums: string[] = [];
      let idx = 0;
      const normalized = key.replace(/[+-]?\d+(?:\.\d+)?/g, (m) => {
        nums.push(m);
        return `{${idx++}}`;
      });
      if (nums.length > 0) {
        const template = map.get(normalized);
        if (template) {
          text = template;
          effectiveArgs = nums;
        }
      }
    }

    if (text === undefined) text = key;

    // 把参数注入翻译模板中的 {N} 占位符
    for (let i = 0; i < effectiveArgs.length; i++) {
      text = text.replace(`{${i}}`, String(effectiveArgs[i]));
    }

    // 去掉 POB 颜色代码 ^xRRGGBB 和 ^N
    text = text.replace(/\^x[0-9A-Fa-f]{6}/g, "").replace(/\^\d/g, "");
    return text;
  }

  return { t };
}
```

- [ ] **Step 2: Commit**

```bash
cd /Users/jason/github/PathOfBuilding
git add web/src/client/hooks/useI18n.ts
git commit -m "feat(web/i18n): support number-normalized pattern lookup in t()"
```

---

### Task 2: WarningsPanel — 翻译警告消息

**Files:**

- Modify: `web/src/client/components/WarningsPanel.tsx`

- [ ] **Step 1: 在 WarningsPanel 中调用 useI18n 并翻译每条 warning**

```typescript
import { Warning as AlertTriangle } from "@phosphor-icons/react"
import type { CalcResult } from "../types"
import { useI18n } from "../hooks/useI18n"

interface Props {
  result: CalcResult
}

export function WarningsPanel({ result }: Props) {
  const { t } = useI18n()

  if (result.warnings.length === 0) return null

  return (
    <div className="p-3 border-t">
      <div className="text-xs font-semibold uppercase tracking-wide text-yellow-500 mb-2">
        警告 <span className="text-[10px] text-yellow-500/50 font-normal normal-case tracking-normal">Warnings</span>
      </div>
      <ul className="flex flex-col gap-1">
        {result.warnings.map((msg, i) => (
          <li key={i} className="flex items-start gap-1.5 text-xs text-yellow-500">
            <AlertTriangle className="h-3 w-3 mt-0.5 shrink-0" />
            <span>{t(msg)}</span>
          </li>
        ))}
      </ul>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add web/src/client/components/WarningsPanel.tsx
git commit -m "feat(web/i18n): translate warning messages"
```

---

### Task 3: SkillsTab — 翻译技能名称和技能组标签

**Files:**

- Modify: `web/src/client/components/SkillsTab.tsx`

- [ ] **Step 1: 在 SkillsTab 和 GemRow 中应用 t()**

`GemRow` 不能直接用 hook（它只是一个内部 React 组件，可以的——但为了减少 hook 数量，在 `SkillsTab` 中调用 `useI18n` 并通过 prop 传递 `t`）。

将 `SkillsTab.tsx` 修改为：

```typescript
import type { BuildConfig, GemInstance } from "../types";
import { Input } from "@/components/ui/input";
import { useI18n } from "../hooks/useI18n";

interface GemRowProps {
  gem: GemInstance;
  t: (s: string) => string;
  onChange: (updated: Partial<GemInstance>) => void;
}

function GemRow({ gem, t, onChange }: GemRowProps) {
  const isSupport = gem.skillId.includes("Support");
  const nameColor = isSupport ? "text-blue-400" : "text-orange-400";
  return (
    <div className="flex items-center gap-2 px-3 py-1 hover:bg-muted/20">
      <span className={`text-xs flex-1 min-w-0 truncate ${nameColor}`}>
        {t(gem.nameSpec || gem.skillId)}
      </span>
      <div className="flex items-center gap-1 shrink-0">
        <span className="text-xs text-muted-foreground">等级</span>
        <Input
          type="number"
          min={1}
          max={23}
          value={gem.level}
          onChange={(e) => onChange({ level: parseInt(e.target.value, 10) })}
          className="w-12 h-6 text-xs text-center px-1"
        />
        <span className="text-xs text-muted-foreground">品质</span>
        <Input
          type="number"
          min={0}
          max={23}
          value={gem.quality}
          onChange={(e) => onChange({ quality: parseInt(e.target.value, 10) })}
          className="w-12 h-6 text-xs text-center px-1"
        />
        <input
          type="checkbox"
          checked={gem.enabled}
          onChange={(e) => onChange({ enabled: e.target.checked })}
          className="h-3.5 w-3.5 shrink-0 accent-primary cursor-pointer"
        />
      </div>
    </div>
  );
}

interface Props {
  buildConfig: BuildConfig;
  onSkillChange: (
    groupIndex: number,
    gemIndex: number,
    updated: Partial<GemInstance>,
  ) => void;
}

export function SkillsTab({ buildConfig, onSkillChange }: Props) {
  const { t } = useI18n();

  return (
    <div className="flex flex-col max-w-xl">
      {buildConfig.skills.length === 0 && (
        <div className="px-3 py-4 text-sm text-muted-foreground">
          没有找到技能配置
        </div>
      )}
      {buildConfig.skills.map((group, gi) => (
        <div key={gi} className="border-t border-border/40 first:border-t-0">
          <div className="px-3 py-1 text-[10px] font-semibold uppercase tracking-wide text-muted-foreground bg-muted/30 border-b border-border/40 flex items-center gap-2">
            <span>{t(group.label) || t(group.slot) || `组 ${gi + 1}`}</span>
            {group.slot && group.label && (
              <span className="text-[10px] text-muted-foreground/50 normal-case tracking-normal font-normal">
                {t(group.slot)}
              </span>
            )}
          </div>
          {group.gems.map((gem, ji) => (
            <GemRow
              key={ji}
              gem={gem}
              t={t}
              onChange={(updated) => onSkillChange(gi, ji, updated)}
            />
          ))}
        </div>
      ))}
    </div>
  );
}
```

注意：`t(group.label) || t(group.slot) || \`组 ${gi + 1}\``中，如果`group.label` 为空字符串，`t("")`返回`""` (falsy)，会正确 fallthrough。

- [ ] **Step 2: Commit**

```bash
git add web/src/client/components/SkillsTab.tsx
git commit -m "feat(web/i18n): translate gem names and skill group labels"
```

---

### Task 4: MainSkillSelector — 翻译主技能选择器中的技能名

**Files:**

- Modify: `web/src/client/components/MainSkillSelector.tsx`

- [ ] **Step 1: 在 getGroupLabel 中应用 t()**

```typescript
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select"
import type { SocketGroup } from "../types"
import { useI18n } from "../hooks/useI18n"

interface Props {
  skills: SocketGroup[]
  mainSocketGroup: number
  onChange: (index: number) => void
}

export function MainSkillSelector({ skills, mainSocketGroup, onChange }: Props) {
  const { t } = useI18n()

  const enabledGroups = skills
    .map((group, i) => ({ group, index: i + 1 }))
    .filter(({ group }) => group.gems.length > 0)

  if (enabledGroups.length === 0) return null

  function getGroupLabel(group: SocketGroup, index: number): string {
    const activeGem = group.gems.find(g => !g.skillId.includes("Support"))
    const gemName = activeGem ? t(activeGem.nameSpec || activeGem.skillId) : ""
    return (group.label ? t(group.label) : "") || gemName || (group.slot ? t(group.slot) : "") || `技能组 ${index}`
  }

  return (
    <div className="px-3 py-2 border-b">
      <div className="text-xs text-muted-foreground mb-1">主要技能 <span className="text-[10px] text-muted-foreground/50">Main Skill</span></div>
      <Select
        value={String(mainSocketGroup)}
        onValueChange={(v) => onChange(parseInt(v, 10))}
      >
        <SelectTrigger className="h-7 text-xs">
          <SelectValue />
        </SelectTrigger>
        <SelectContent>
          {enabledGroups.map(({ group, index }) => (
            <SelectItem key={index} value={String(index)} className="text-xs">
              {getGroupLabel(group, index)}
            </SelectItem>
          ))}
        </SelectContent>
      </Select>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add web/src/client/components/MainSkillSelector.tsx
git commit -m "feat(web/i18n): translate skill names in main skill selector"
```

---

### Task 5: ItemsTab — 翻译装备名称和词缀

**Files:**

- Modify: `web/src/client/components/ItemsTab.tsx`

- [ ] **Step 1: 在 ItemsTab 顶层调用 useI18n，传递 t 到 SlotGroup 和 ItemDetail**

关键修改点：

1. `ItemsTab` 中 `const { t } = useI18n()`
2. `SlotGroup` 组件增加 `t` prop，用于翻译 `item.name || item.base`
3. `ItemDetail` 组件增加 `t` prop，用于翻译 `item.name`、`item.base`、每条 mod

**SlotGroup**（左侧列表）修改 item 名称显示行：

```typescript
// 原来
{
  item ? item.name || item.base : "空";
}

// 改为（在 SlotGroupProps 中加 t prop）
{
  item ? t(item.name || item.base) : "空";
}
```

**ItemDetail**（右侧详情）修改：

```typescript
// item 名称行
<div className={`text-base font-semibold leading-tight ${RARITY_COLORS[item.rarity] ?? ""}`}>
  {t(item.name || item.base)}
</div>
{item.name && item.base && (
  <div className="text-sm text-muted-foreground">{t(item.base)}</div>
)}

// implicits
{mods.implicits.map((mod, i) => (
  <div key={`imp-${i}`} className="text-xs text-yellow-300/80 leading-snug">
    {t(mod)}
  </div>
))}

// explicits
{mods.explicits.map((mod, i) => (
  <div key={`exp-${i}`} className={`text-xs ${modColor} leading-snug`}>
    {t(mod)}
  </div>
))}
```

完整修改：在 `SlotGroupProps` 中加 `t: (s: string) => string`，在 `ItemDetailProps` 中加 `t: (s: string) => string`，并在 `ItemsTab` 组件中调用 `useI18n` 后把 `t` 传给这两个子组件。

- [ ] **Step 2: Commit**

```bash
git add web/src/client/components/ItemsTab.tsx
git commit -m "feat(web/i18n): translate item names and mods in items tab"
```

---

### Task 6: StatsPanel — 翻译 breakdown 行

**Files:**

- Modify: `web/src/client/components/StatsPanel.tsx`

- [ ] **Step 1: 在 StatsPanel 中调用 useI18n，传 t 到 StatRow**

`StatRow` 组件接受新 prop `t`，用于翻译 `entry.label`：

```typescript
// StatRowProps 加 t
interface StatRowProps {
  label: string;
  value: number;
  fmt: string;
  breakdown?: BreakdownLine[];
  t: (s: string) => string;
}

// StatRow 中 breakdown 行
{breakdown.map((entry, i) => (
  <div key={i} className="text-xs text-muted-foreground font-mono">
    {t(entry.label)}
  </div>
))}
```

`StatsPanel` 顶层：`const { t } = useI18n()`，然后把 `t` 传给 `StatGroup`，`StatGroup` 再传给 `StatRow`。

- [ ] **Step 2: Commit**

```bash
git add web/src/client/components/StatsPanel.tsx
git commit -m "feat(web/i18n): translate breakdown lines in stats panel"
```

---

### Task 7: CalcsTab — 翻译 breakdown 行

**Files:**

- Modify: `web/src/client/components/CalcsTab.tsx`

- [ ] **Step 1: 在 CalcsTab 中调用 useI18n，传 t 到 StatRow/StatCell**

与 StatsPanel 同样的模式：CalcsTab 顶层 `const { t } = useI18n()`，经由 `Section` prop 传到 `StatRow` 和 `StatCell`，对每个 `line.label` 调用 `t(line.label)`。

需要修改的 prop chain：

- `CalcsTab` → `Section`（新增 `t` prop）→ `StatRow` 和 `StatCell`（新增 `t` prop）

每个 breakdown popover 内容：

```typescript
{bkLines.map((line, i) => (
  <div key={i} className="text-xs text-muted-foreground font-mono leading-snug">
    {t(line.label)}
  </div>
))}
```

- [ ] **Step 2: Commit**

```bash
git add web/src/client/components/CalcsTab.tsx
git commit -m "feat(web/i18n): translate breakdown lines in calcs tab"
```

---

## 关于 CSV 扩展

实现完成后，向 CSV 文件加入新词条即可立即生效：

- **技能名称**：加入 `i18n/zh-CN/Items_Gems.csv`
- **装备词缀（无数字）**：加入 `i18n/zh-CN/Items_Gems.csv`
- **装备词缀（含数字）**：key 使用 `{0}`, `{1}` 占位符，如：
  ```csv
  "+{0} to maximum Life","+{0} 最大生命"
  "Adds {0} to {1} Fire Damage to Attacks","造成 {0} 到 {1} 点火焰伤害到攻击"
  "{0}% increased Attack Speed","{0}% 提高攻击速度"
  ```
- **警告消息**：加入 `i18n/zh-CN/GUI.csv` 或新建 `Warnings.csv`

---

## 验证方法

1. `bun run dev` 启动服务
2. 加载一个带有宝石和装备的 Build
3. 技能 Tab：技能名应显示中文
4. 装备 Tab：装备名和词缀应显示中文（前提是 CSV 中有对应翻译）
5. 警告面板：警告消息应翻译
6. Breakdown popover：breakdown 行应翻译
